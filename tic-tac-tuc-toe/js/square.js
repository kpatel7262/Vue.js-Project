class Square{
    constructor(){
        this.makeMove = function(i){
            this.value = null; //no value when the game starts
            this.highlight = false; //highligted if there is a winner
        }
    }
}